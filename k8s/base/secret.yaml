apiVersion: v1
kind: Secret
metadata:
  name: shopsphere-secrets
  namespace: shopsphere
type: Opaque
# Values are base64 encoded. Generate with:  echo -n 'yourvalue' | base64
# In Step 7 (Security), these will be pulled from AWS Secrets Manager instead
stringData:
  # RDS connection string from terraform output: rds_endpoint
  # Format: postgresql://username:password@host:5432/dbname
  DATABASE_URL: "postgresql://shopsphere_admin:SaurabhGuptaAWS123!@shopsphere-postgres.chyua8qqkum0.ap-south-1.rds.amazonaws.com:5432/shopsphere"

  # Simple API key for inter-service auth (upgrade to JWT in Step 7)
  INTERNAL_API_KEY: "dev-internal-key-change-in-production"